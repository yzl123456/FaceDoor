<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.facedoor.slowlife">

    <!-- camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- sdcard -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- wifi internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="注册机_门禁客户端"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:replace="android:name,android:theme">
        <activity android:name=".PermissionAcitivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".DetecterActivity" />
        <activity
            android:name=".MainActivity2"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".RegisterMachineActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RMIndexActivity" />
        <activity android:name=".GuestInfoActivity" />
        <activity android:name=".OtherInfoActivity" />
        <activity android:name=".WorkerInfoActivity" />
        <activity android:name=".OwnerInfoActivity" />
        <activity android:name=".RMFaceUpdateActivity" />
        <activity android:name=".RMManInfoActivity" />
        <activity android:name=".OpenSuccessActivity" />
    </application>

</manifest>